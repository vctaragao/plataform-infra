apiVersion: configuration.konghq.com/v1
kind: KongClusterPlugin
metadata:
  name: hello-world-jwt-auth
  annotations:
    kubernetes.io/ingress.class: kong
plugin: jwt
config:
  claims_to_verify:
    - exp
    - nbf
